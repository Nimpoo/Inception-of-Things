Vagrant.configure("2") do |config|
  # * Provider-specific configuration
  config.vm.provider :libvirt do |libvirt|
    libvirt.memory = 2048 # * 2GB of memory
    libvirt.cpus = 2      # * 2 cores
    libvirt.qemu_use_session = false
    libvirt.driver = "kvm"
  end

  # * Box distribution
  config.vm.box = "generic/ubuntu2204"
  config.vm.box_version = "4.3.12"

  # * NFS synced folder configuration
  config.vm.synced_folder "synced_folder_nfs/", "/synced_folder_nfs/",
    type: "nfs",
    nfs_version: 4

  # * Provisioning all VMs
  config.vm.provision "upgrade and firewall",
    type: "shell",
    inline: <<-SHELL
    apt-get update

    ufw allow 6443/tcp                 # Api Server
    ufw allow from 10.42.0.0/16 to any # Pods
    ufw allow from 10.43.0.0/16 to any # Services
  SHELL

  # * Base box configuration
  config.vm.define "noalexanS" do |noalexanS|
    noalexanS.vm.hostname = "noalexanS"

    # * Network configuration
    noalexanS.vm.network :private_network, ip: "192.168.56.110",
      libvirt__host_ip: "192.168.56.1",
      libvirt__network_name: "vagrant-libvirt-p1",
      libvirt__dhcp_enabled: false

    # * K3s installation in controller mode
    noalexanS.vm.provision "k3s - controller mode",
      type: "shell",
      inline: <<-SHELL
        export INSTALL_K3S_EXEC="--write-kubeconfig-mode=644 --tls-san $(hostname) --node-ip 192.168.56.110  --bind-address=192.168.56.110 --advertise-address=192.168.56.110"
        curl -sfL https://get.k3s.io | sh -
        sed -i 's/127.0.0.1/192.168.56.110/g' /etc/rancher/k3s/k3s.yaml
        cp -v /var/lib/rancher/k3s/server/node-token /etc/rancher/k3s/k3s.yaml /synced_folder_nfs/
      SHELL
  end

  config.vm.define "mayoubSW" do |mayoubSW|
    mayoubSW.vm.hostname = "mayoubSW"

    # * Network configuration
    mayoubSW.vm.network :private_network, ip: "192.168.56.111",
      libvirt__host_ip: "192.168.56.1",
      libvirt__network_name: "vagrant-libvirt-p1",
      libvirt__dhcp_enabled: false

    # * K3s installation in agent mode
    mayoubSW.vm.provision "k3s - agent mode",
      type: "shell",
      inline: <<-SHELL
        while [ ! -e /synced_folder_nfs/node-token ]; do sleep 10; done;
        mkdir -pv /home/vagrant/.kube
        cp -v /synced_folder_nfs/k3s.yaml /home/vagrant/.kube/config
        chown -v vagrant:vagrant /home/vagrant/.kube/config
        export INSTALL_K3S_EXEC="agent --server https://192.168.56.110:6443 --token-file /synced_folder_nfs/node-token --node-ip=192.168.56.111"
        curl -sfL https://get.k3s.io | sh -
      SHELL
  end
end
